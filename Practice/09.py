# Uses built-in functions to print:

# The sum of all elements
# The maximum number
# The sorted list in descending order

numbers = [3, 7, 1, 9, 4]

print(sum(numbers))

print(max(numbers))

print(sorted(numbers)) # ascending
print(sorted(numbers, reverse=True)) # descending