# Write a Python function called calculate_area that:

# Takes two parameters: length and width.
# Returns the area of a rectangle.
# Call the function with sample values and print the result.

def calculate_area(length, width):
    area = length * width
    return area

area = calculate_area(4, 3)
print("area", area)